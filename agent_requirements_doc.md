# Agent 平台功能升级与学习实操需求文档 (PRD)

## 1. 文档概述
本文件旨在定义 `AgentChat` 平台的进阶功能开发需求。通过对现有对话功能的升级，实现从简单的“模型对话”向“自主 Agent”架构的转型，并作为开发者的学习实操路径。

## 📌 需求完成度概览 (截至 2026-05-22)
> [!NOTE]
> 🟢 **已完成 (Fully Completed)** | 🟡 **部分完成 (Partially Completed)** | 🔴 **未完成 (Not Started)**

*   **[🟢 已完成] 3.1 角色定制化 (Persona & System Prompt)**: 后端存储、Agent 实例化、前端模板交互均已全链路打通。
*   **[🔴 未完成] 3.2 工具集成与能力扩展 (Tool Calling)**: 数据库、后端注解、自动反射调用及前端授权均未实现。*(本次已补充具体工具开发优先级与超时控制策略)*
*   **[🔴 未完成] 3.3 智能记忆管理 (Memory Management)**: 滑动窗口、摘要持久化机制未实现。*(本次已补充单 Agent 容量配置化细则)*
*   **[🟢 已完成] 3.4 推理过程可视化 (Reasoning Display)**: 后端已集成 AgentScope Studio 可视化面板，且前端 `agent-client` 已完美支持推理流式解析、打字机队列动效与 UI 折叠展示，并在异常情况下支持降级展示。
*   **[🟢 已完成] 6. 实施路线图 (Milestones) - T1**: 基础角色定制与持久化已交付。
*   **[🔴 未完成] 6. 实施路线图 (Milestones) - T2~T6**: 后续工具闭环、推理可视化、RAG、HITL、多智能体等里程碑未开始。
*   **[🔴 未完成] 7. 进阶与高级需求 (Advanced Features)**: 包含 RAG、多智能体协作、规划与反思、人机协同审批、工作流引擎均未实现。
*   **[🟡 部分完成] 8. 开源工程化与快速启动 (Open Source & Quick Start)**: 本地配置模板（`application.yml`）已完善，但一键 Docker 部署和预检机制未完成。
*   **[🟡 部分完成] 9. 资源库与内置插件 (Asset Library & Built-in Plugins)**: 前端已预置 3 个常用 Agent 模板（翻译官、代码专家、心理医生），但 10+ 模板市场及 SerpApi、Python 解释器等常用工具集未内置。
*   **[🟡 部分完成] 10. 交互体验深度优化 (UX Deep Optimization)**: 前端 Markdown、代码高亮、暗黑模式均已支持；LaTeX、Mermaid、Token 成本监控、对话导出未实现。
*   **[🟡 部分完成] 11. 开发者生态与安全 (Developer Ecosystem & Security)**: 原生模型聚合接入 Qwen/DeepSeek/OpenAI 已完成；Java 插件 SDK、链路 Tracing 面板、LocalStorage 隔离等未完成。
*   **[🔴 未完成] 12. 用户认证与权限管理 (Auth & Permission)**: JWT 登录体系及多租户隔离机制未完成。*(本次新增)*
*   **[🔴 未完成] 13. 可观测性与运维 (Observability & Ops)**: 结构化日志、Spring Boot Actuator 性能监控等未完成。*(本次新增)*
*   **[🔴 未完成] 14. 流式对话的健壮性优化 (SSE Reliability)**: 前端断线重连、流控与限流等未完成。*(本次新增)*
*   **[🔴 未完成] 15. 测试体系 (Testing Strategy)**: 自动化单元测试覆盖率及 Mock integration 测试未完成。*(本次新增)*
*   **[🔴 未完成] 16. 企业级开箱即用增强需求 (Enterprise Out-of-the-Box Enhancements)**: 针对企业级私有化、数据安全脱敏及多通道发布等扩展功能未完成。*(本次新增)*

---

## 2. 目标用户
*   **初级 AI 开发者**：希望通过实操掌握 Agent 开发的核心概念。
*   **平台使用者**：需要能够自定义能力、具备专业背景知识的智能助手。

## 3. 功能需求详情

### 3.1 角色定制化 (Persona & System Prompt) 【🟢 已完成】
*   **背景**：目前创建 Agent 仅支持名称和模型选择，无法设定其行为模式。
*   **需求点**：
    *   `[x]` **后端存储**：在 `AgentInfo` 实体中增加 `system_prompt` 字段。*(已完成，支持持久化至 t_agent_info)*
    *   `[x]` **Agent 实例化**：在构建 `ReActAgent` 时，必须注入 `system_prompt` 作为 LLM 的初始指令。*(已完成，在 buildAgent 中注入)*
    *   `[x]` **前端交互**：在“创建 Agent”弹窗中，提供多行文本框输入系统提示词，并支持预设模板（如：翻译官、代码专家、心理医生）。*(已完成，CreateAgentModal.vue 中已内置此交互)*

### 3.2 工具集成与能力扩展 (Tool Calling) 【🔴 未完成】
*   **背景**：Agent 需要具备操作外部世界的能力（如查询天气、搜索网页、执行计算等），ReAct 架构通过 Tool Calling 实现这一闭环。
*   **需求点**：
    *   `[ ]` **1. 后端工具开发规范**：
        *   `[ ]` **注解驱动**：定义 `@AgentTool` 注解，用于标记可被 Agent 调用的 Java 方法。
        *   `[ ]` **参数描述**：注解需包含工具名称、功能描述及参数说明（JSON Schema 格式），以便 LLM 理解如何调用。
        *   `[ ]` **框架集成**：在 `AgentBizImpl` 构建 `ReActAgent` 时，动态注入当前 Agent 关联的工具实例。
        *   `[ ]` **工具执行超时策略**：设置工具执行超时上限（默认 10 秒），超时自动熔断并向 LLM 返回 Timeout Observation，防止请求被挂死。 *(补充)*
        *   `[ ]` **内置工具开发优先级**：定义首批实现 Weather Tool（天气工具）、Calculator Tool（计算工具）、Web Search Tool（网页搜索工具）。 *(补充)*
    *   `[ ]` **2. 数据库设计**：
        *   `[ ]` **工具元数据表** (`t_tool_config`)：存储系统中所有可用的工具信息（名称、类路径、方法名、描述）。
        *   `[ ]` **Agent-工具关联表** (`t_agent_tool_rel`)：实现 Agent 与工具的多对多关联。
    *   `[ ]` **3. 前端交互设计**：
        *   `[ ]` **工具市场/列表**：在创建或编辑 Agent 的界面，展示系统已注册的工具列表。
        *   `[ ]` **勾选授权**：用户通过复选框为特定 Agent 分配工具使用权限。
        *   `[ ]` **实时反馈**：在对话过程中，如果触发了工具调用，前端需展示“正在调用工具 [XXX]...”的状态。
    *   `[ ]` **4. 执行逻辑与安全性**：
        *   `[ ]` **自动执行**：后端拦截 LLM 的 `Action` 输出，自动反射调用对应方法。
        *   `[ ]` **异常处理**：若工具执行失败，需将错误信息包装成 `Observation` 返回给 LLM，使其能够尝试重试或修正。

### 3.3 智能记忆管理 (Memory Management) 【🔴 未完成】
*   **背景**：长对话会导致 Token 溢出或响应变慢。
*   **需求点**：
    *   `[ ]` **滑动窗口记忆**：仅保留最近 N 轮对话作为上下文。
    *   `[ ]` **摘要持久化**：当对话轮数超过阈值，调用 LLM 对历史进行摘要总结，减少上下文长度。
    *   `[ ]` **单 Agent 容量独立配置**：支持在创建 Agent 时为每个 Agent 单独配置保留的历史对话轮数上限（如 5/10/20 轮）。 *(补充)*

### 3.4 推理过程可视化 (Reasoning Display) 【🟢 已完成】
*   **背景**：Agent 的思考过程（Thought）目前对用户不可见，无法判断其逻辑。
*   **需求点**：
    *   `[x]` **流式输出解析**：前端开发了高容错的双重协议解析器，实时切分 DeepSeek `<think>` 及 ReAct 标签。
    *   `[x]` **UI 展示**：前端支持磨砂玻璃质感的独立可折叠手风琴面板，配合 Chevron 旋转动效与 15ms 智能变速打字机渲染。
    *   `[x]` **异常与错误降级展示**：错误与连接断开时，打字机自动终止并降级以红色警示框/提示直接显式展示，不发生白屏或死锁。
    *   `[x]` **后端集成**：后端已集成官方的 AgentScope Studio，并在 `agentscope.studio.enabled=true` 时自动初始化 `StudioMessageHook` 实时同步推理过程。*(已完成)*

## 4. 技术栈要求
*   **后端**：Java 17+, Spring Boot 3.x, AgentScope SDK
*   **前端**：Vue 3 (Vite), TypeScript, CSS3
*   **模型**：支持 OpenAI 协议的模型（如 DeepSeek, Qwen）

## 5. 验收标准
1.  `[x]` **角色一致性**：创建特定角色的 Agent 后，其回答风格需严格符合 Prompt 设定。*(已通过测试验证)*
2.  `[ ]` **工具有效性**：询问实时信息时，Agent 能够正确触发工具调用并给出准确回复。*(待工具集成完成后验证)*
3.  `[ ]` **流程完整性**：前端能够清晰展示 Agent 从接收问题、思考、执行工具到最终回复的完整链路。*(待推理过程解析渲染完成后验证)*

---

## 6. 实施路线图 (Milestones)
*   **[🟢 T1 (Phase 1)]**: 完成基础角色定制与持久化。*(已完成)*
*   **[🔴 T2 (Phase 2)]**: 实现第一个核心工具（如 Weather Tool）的闭环调用。*(未完成)*
*   **[🔴 T3 (Phase 3)]**: 优化前端 UI，支持推理链路展示。*(未完成)*
*   **[🔴 T4 (Phase 4)]**: 引入私有知识库 (RAG)，赋予 Agent 检索本地文档的能力。*(未完成)*
*   **[🔴 T5 (Phase 5)]**: 实现人机协同 (Human-in-the-loop)，为高敏感工具（如数据库修改）增加人工审批流程。*(未完成)*
*   **[🔴 T6 (Phase 6)]**: 探索多智能体 (Multi-Agent) 协作与工作流编排，解决复杂多步任务。*(未完成)*

---

## 7. 进阶与高级需求 (Advanced Features) 【🔴 未完成】

### 7.1 检索增强生成 (RAG - Knowledge Base) 【🔴 未完成】
*   **背景**：Agent 需要结合企业的私有数据或最新的外部文档来回答问题，避免产生“幻觉”。
*   **需求点**：
    *   `[ ]` **文档解析与向量化**：支持上传文本/PDF文档，利用 Embedding 模型转换为向量并存储到向量数据库（如 Milvus / Chroma）。
    *   `[ ]` **检索与挂载**：在 Agent 执行过程中，先根据用户问题在向量库中进行语义检索 (Similarity Search)，将相关内容作为 Context 挂载到 Prompt 中再交给大模型。

### 7.2 多智能体协作 (Multi-Agent Collaboration) 【🔴 未完成】
*   **背景**：针对复杂的业务场景，单一的 Agent 往往难以兼顾所有逻辑，需要多个专职 Agent 协同工作。
*   **需求点**：
    *   `[ ]` **角色编排**：设计不同的 Agent（如 Coder Agent, Reviewer Agent, QA Agent）。
    *   `[ ]` **消息路由**：实现一个中控（Manager Agent）或者流水线机制，让不同的 Agent 能够互相传递消息、相互纠错。

### 7.3 高级规划与反思机制 (Planning & Reflection) 【🔴 未完成】
*   **背景**：基础的 ReAct 模式在面对长链路任务时容易迷失方向。
*   **需求点**：
    *   `[ ]` **任务拆解 (Plan-and-Solve)**：Agent 在行动前必须先输出一份 Step-by-step 的计划书。
    *   `[ ]` **自我纠错 (Self-Correction)**：在工具调用失败或结果不符合预期时，Agent 能够根据错误信息反思，并尝试采用替代方案而不是直接报错。

### 7.4 人机协同与安全审批 (Human-in-the-loop, HITL) 【🔴 未完成】
*   **背景**：Agent 操作外部系统的权限过大存在安全隐患，某些操作必须由人类确认。
*   **需求点**：
    *   `[ ]` **执行挂起**：当检测到敏感工具调用（如发送邮件、系统写操作）时，后端将状态机挂起，并推送审批卡片到前端。
    *   `[ ]` **人工干预**：人类点击“同意”、“拒绝”或“修改参数”后，唤醒后端状态机继续后续逻辑。

### 7.5 可控工作流引擎 (Workflow Engine) 【🔴 未完成】
*   **背景**：在生产环境中，完全由 LLM 驱动的 Agent 往往不够稳定，需要与传统的可控业务流结合。
*   **需求点**：
    *   `[ ]` **图结构执行**：通过有向无环图 (DAG)编排 Agent 的执行路径（类似 LangGraph）。
    *   `[ ]` **条件流转**：结合传统的 If/Else 逻辑和 LLM 的意图识别，使得流程既具备智能化又有确定的边界。

---

## 8. 开源工程化与快速启动 (Open Source & Quick Start) 【🟡 部分完成】
*   **目标**：实现“5分钟上手”，降低部署门槛。
*   **需求点**：
    *   `[ ]` **Docker 一键部署**：提供 `docker-compose.yml`，包含后端、前端、数据库及向量库。
    *   `[ ]` **环境预检机制**：系统启动时自动检查数据库连接、API Key 有效性及必要的存储目录权限。
    *   `[x]` **配置模版化**：提供详尽的 `application.yml` 示例配置以及后端各模块划分结构。*(已完成)*
    *   `[ ]` **演示模式 (Demo Mode)**：内置 Mock 数据 and Mock Model，允许用户在未配置 API Key 的情况下体验 UI 交互。

## 9. 资源库与内置插件 (Asset Library & Built-in Plugins) 【🟡 部分完成】
*   **目标**：消除冷启动焦虑，提供开箱即用的能力。
*   **需求点**：
    *   `[x]` **Agent 模版预置**：预置 3+ 高质量 Agent（如：代码专家、翻译官、心理医生）。*(已完成，支持前台直接选用模板创建)*
    *   `[ ]` **Agent 模版市场**：扩充至 10+ 预置 Agent 供用户检索及一键克隆。
    *   `[ ]` **常用工具集**：内置 Google Search (SerpApi)、Python 解释器（沙箱运行）、文件读取器、精准计算器。
    *   `[ ]` **Prompt 优化助手**：内置一个专门优化提示词的 Agent，帮助用户将模糊需求转化为专业的 System Prompt。

## 10. 交互体验深度优化 (UX Deep Optimization) 【🟡 部分完成】
*   **目标**：打造 premium 级别的视觉与交互感受。
*   **需求点**：
    *   `[x]` **基础渲染与代码高亮**：完美支持 Markdown 和 `highlight.js` 实现的代码高亮。*(已完成)*
    *   `[ ]` **高级渲染支持**：支持 LaTeX 公式、Mermaid 流程图。
    *   `[ ]` **Token & 成本监控**：在对话侧边栏实时展示单次会话及累计的 Token 消耗与估算成本。
    *   `[x]` **全平台适配**：支持精致的暗黑模式，响应式适配移动端和桌面端浏览器。*(已完成)*
    *   `[ ]` **对话导出**：支持将会话导出为图片、Markdown 或 PDF 文件。

## 11. 开发者生态与安全 (Developer Ecosystem & Security) 【🟡 部分完成】
*   **目标**：吸引二次开发，确保私有化部署安全。
*   **需求点**：
    *   `[ ]` **插件 SDK**：定义标准 Java 接口与注解，开发者只需编写单一类即可自动在前端注册新工具。
    *   `[x]` **模型聚合接入**：原生支持 OpenAI、DeepSeek、Qwen 阿里云等多种大模型一键配置与统一调用.*(已完成)*
    *   `[ ]` **调试监控面板**：内置轻量级链路追踪 (Tracing)，展示 LLM 的原始 Prompt、调用耗时及命中上下文。
    *   `[ ]` **数据脱敏与安全**：支持 API Key 前端 LocalStorage 隔离存储，日志自动遮蔽敏感信息。
    *   `[ ]` **完善的贡献指南**：提供 `CONTRIBUTING.md`，明确说明如何贡献新 Agent 或新工具。

---

## 12. 用户认证与权限管理 (Auth & Permission) 【🔴 未完成】
*   **目标**：构建安全的身份体系，保障多租户资源隔离。
*   **需求点**：
    *   `[ ]` **登录鉴权体系**：在后端引入 Spring Security 或 JWT（目前 `t_user` 表已在 `init.sql` 中建好但没有逻辑层接入）。
    *   `[ ]` **数据多租户隔离**：在数据库交互与 API 调用时严格校验用户身份（目前仅使用 `X-User-Id` Header 并不安全），防止未授权读取他人的 Agent、会话或历史记录。
    *   `[ ]` **Agent 分享与克隆**：支持用户将自己定制的优秀 Agent 发布给公共市场，其他用户可以一键添加或克隆。

## 13. 可观测性与运维 (Observability & Ops) 【🔴 未完成】
*   **目标**：提升系统生产运维能力与故障排查效率。
*   **需求点**：
    *   `[ ]` **结构化日志输出**：统一定制后端的 Logback 输出格式为结构化 JSON，便于接入 ELK、Loki 等日志收集分析工具。
    *   `[ ]` **Spring Boot Actuator 服务监控**：集成端点以暴露系统健康状态 `/actuator/health`、JVM 指标等。
    *   `[ ]` **调用链路耗时指标**：收集每次 LLM API 调用以及工具调用的具体耗时并输出至度量库中（如 Prometheus），通过 Grafana 实现可视化监控。

## 14. 流式对话的健壮性优化 (SSE Reliability) 【🔴 未完成】
*   **目标**：保障在恶劣网络环境或高并发负载下的稳定对话体验。
*   **需求点**：
    *   `[ ]` **前端断线智能重连**：当客户端网络发生抖动引起 SSE 流中断时，触发指数退避机制自动尝试重新发起流请求。
    *   `[ ]` **高并发流控限流 (Rate Limiting)**：在后端网关或控制器层为每名用户设置流式对话请求并发限流（如基于 Bucket4j 或 Redis 实现的令牌桶策略），防止恶意轰炸或压垮大模型 API。
    *   `[ ]` **请求重连续传机制**：对中断的大模型长文本输出进行断点继续缓存。

## 15. 测试体系 (Testing Strategy) 【🔴 未完成】
*   **目标**：保证核心逻辑稳定性，实现高质量的 CI/CD 自动化集成。
*   **需求点**：
    *   `[ ]` **高单元测试覆盖率**：为 backend 核心服务层及工具反射管理类编写完善的 JUnit 测试，测试覆盖率不低于 60%。
    *   `[ ]` **大模型 Mock 测试框架**：引入 Mockito 能够脱离大模型网络接口模拟各种 LLM 的返回（包括正常回复、流式数据、工具调用 Action 等），作为 CI 必经关卡。
    *   `[ ]` **前端 E2E 自动化测试**：完善 Playwright 测试脚本以覆盖注册登录、创建 Agent、与 Agent 进行对话的完整 UI 闭环流。

---

## 16. 企业级开箱即用增强需求 (Enterprise Out-of-the-Box Enhancements) 【🔴 未完成】
*   **目标**：满足企业私有化部署、安全合规、多渠道分发及精细化成本管控的需求，提供即插即用的企业版体验。
*   **需求点**：
    *   `[ ]` **1. 企业单点登录 (SSO) 与组织架构同步**：
        *   支持 LDAP / Active Directory (AD) 目录服务接入。
        *   集成主流企业协同办公平台（如**飞书、钉钉、企业微信**）的 OAuth2.0 免登，并自动同步组织架构与部门人员关系。
    *   `[ ]` **2. 企业数据防泄露 (DLP) 与合规审计**：
        *   **敏感数据拦截 (PII Scrubbing)**：在将 Prompt 发送至外部公有云大模型前，自动通过正则或本地分类器对敏感数据（如手机号、身份证、内部代码、敏感资产机密等）进行脱敏或遮蔽。
        *   **全量对话审计日志**：系统保留所有员工的提问和 Agent 答复日志（包括触发的工具与检索到的文档），支持安全合规团队的随时检索、审计与回溯。
    *   `[ ]` **3. 细粒度成本监控与配额限制**：
        *   **Token 额度管控**：企业可以为不同部门（如研发部、市场部）或单名员工设置月度/每日 Token 消费配额。
        *   **API 密钥轮转与高可用**：后端集成高可用 API 代理，支持配置多个大模型 API 密钥并自动根据 Rate Limit 状态轮询切换，防止大批量员工使用时发生断流。
    *   `[ ]` **4. 知识库 (RAG) 文档级精细化权限**：
        *   支持将不同知识分类挂载到不同权限组（例如：财务部涉密文档仅允许被“财务助理 Agent”读取，且该 Agent 仅对财务部群组开放可见）。
        *   提供主流协同文档连接器（Feishu Wiki, SharePoint, Confluence）的数据源定时增量同步更新。
    *   `[ ]` **5. 多端多渠道一键发布 (Multi-Channel Publishing)**：
        *   支持将创建好的 Agent 一键发布为飞书群机器人、钉钉群机器人、企业微信应用机器人。
        *   提供类似网页 Web Widget 的 JS 代码挂件，方便企业一键嵌入已有的内部门户（如 HR 系统、IT 工单系统等）。
    *   `[ ]` **6. 私有化/局域网离线部署包**：
        *   提供一整套离线安装方案（集成 Ollama 或 vLLM 大模型端点），使用户能在完全断网的物理隔离局域网中运行完整的 Agent 平台。
        *   集成轻量级嵌入式向量数据库（如 SQLite-Vec 或 HNSWLib）和本地 Embedding 模型。
